#!/bin/bash

source $(dirname $(dirname $(realpath ${BASH_SOURCE[0]})))/conf/env || exit 1
source ${CRON_CONF}/init || exit 1

mysql SkillsBriefcase < ./SkillsBriefcase_schema.sql
mysql SkillsBriefcase < ./SkillsBriefcase_data.sql
