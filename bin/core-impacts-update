#!/bin/bash

source $(dirname $(dirname $(realpath ${BASH_SOURCE[0]})))/conf/env || exit 1
export CRON_APP_DIR=${CRON_APPS}/core-impacts-import
source ${CRON_APP_DIR}/init || exit 1

./bin/console -vvv app:sync-course-sections
